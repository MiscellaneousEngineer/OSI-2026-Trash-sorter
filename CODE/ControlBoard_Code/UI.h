// void drawRectWH(uint8_t x, uint8_t y, uint8_t w, uint8_t h, uint16_t color)
// void fillRect(uint8_t x, uint8_t y, uint8_t w, uint8_t h, uint16_t color)
// void drawText(uint8_t x, uint8_t y, const char *_text, uint16_t color, uint16_t bg, uint8_t size)
// to_ms_since_boot(get_absolute_time())

/*void drawAMP(uint16_t X, uint16_t Y, uint16_t Amperage, bool good, uint32_t time)
{ // Kevlin overcurrent measurment displays

    if (good)
    { // If no overcurrent
        drawRectWH(X, Y, 20, 14, ST7735_GREEN);
        fillRect(X + 1, Y + 1, 18, 12, ST7735_WHITE);
        drawText(X + 2, Y + 2, Amperage, ST7735_BLACK, ST7735_WHITE);
    }
    if (!good)
    {
        if
        { // switch colours
        }
    }
}*/

void ActuatorStatus

void MotorStatus

